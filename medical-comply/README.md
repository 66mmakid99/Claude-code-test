# 🏥 MedicalComply - 의료법 준수 검사 서비스

병원/의료기관 웹사이트의 **의료법 제56조** 준수 여부를 AI로 자동 검사하는 SaaS 서비스입니다.

## 주요 기능

| 기능 | 설명 |
|------|------|
| **웹사이트 검사** | URL 입력 → 자동 크롤링 → 위반 사항 분석 |
| **AI 분석** | Claude API를 활용한 정밀 분석 |
| **PDF 리포트** | 상세 분석 결과 PDF 다운로드 |
| **이메일 발송** | 리포트 이메일 발송 |
| **딜러 시스템** | 추천인 50% 수수료 |
| **결제 연동** | 토스페이먼츠 구독 결제 |

## 의료법 검사 항목 (제56조)

| 코드 | 항목 | 심각도 |
|------|------|--------|
| MED001 | 환자 후기/치료 경험담 게시 | 위반 |
| MED002 | 치료 전후 비교 사진 | 위반 |
| MED003 | 성공률/효과 보장 표현 | 위반 |
| MED004 | 유명인/연예인 추천 | 경고 |
| MED005 | 과도한 할인/이벤트 광고 | 경고 |
| MED006 | 최상급 표현 (최고, 1위 등) | 경고 |

---

## 🚀 빠른 시작

### 1. 의존성 설치

```bash
# 백엔드
cd medical-comply/backend
npm install

# 프론트엔드
cd ../frontend
npm install
```

### 2. 개발 서버 실행

**방법 1: 스크립트 사용**
```bash
cd medical-comply
chmod +x start-dev.sh
./start-dev.sh
```

**방법 2: 수동 실행**
```bash
# 터미널 1 - 백엔드
cd medical-comply/backend
npm run dev

# 터미널 2 - 프론트엔드
cd medical-comply/frontend
npm run dev
```

### 3. 접속

- **프론트엔드**: http://localhost:5173
- **백엔드 API**: http://localhost:5000

---

## 🧪 테스트 모드

API 키나 데이터베이스 없이도 모든 기능을 테스트할 수 있습니다.

### Mock DB 모드
- `DATABASE_URL` 환경변수가 없으면 자동으로 Mock DB 모드로 실행
- 메모리 내 데이터 저장 (서버 재시작 시 초기화)

### 테스트 결제
1. 요금제 페이지 → "테스트 모드" 체크박스 활성화
2. 결제하기 클릭 → 실제 결제 없이 구독 활성화

### 테스트 시나리오

```
1. 회원가입 (http://localhost:5173/register)
   - 이메일: test@example.com
   - 비밀번호: password123
   - 이름: 테스트유저

2. 웹사이트 검사 (대시보드)
   - URL 입력: https://example.com
   - 검사 시작 클릭
   - 결과 확인 (점수, 위반사항)

3. PDF 다운로드
   - 검사 결과 상세 페이지
   - "PDF 다운로드" 버튼 클릭

4. 테스트 결제
   - 요금제 페이지
   - 테스트 모드 활성화
   - 결제하기 클릭
```

---

## 📁 프로젝트 구조

```
medical-comply/
├── backend/
│   ├── config/
│   │   ├── database.js       # DB 연결 (Mock 모드 지원)
│   │   └── schema.sql        # PostgreSQL 스키마
│   ├── routes/
│   │   ├── auth.js           # 인증 (회원가입/로그인)
│   │   ├── reports.js        # 검사 및 리포트
│   │   ├── payments.js       # 결제 (테스트 모드 지원)
│   │   └── dealers.js        # 딜러 대시보드
│   ├── services/
│   │   ├── crawler.js        # Puppeteer 크롤러
│   │   ├── analyzer.js       # 위반 분석 엔진
│   │   ├── pdf-generator.js  # PDF 생성
│   │   └── email-sender.js   # 이메일 발송
│   ├── middlewares/
│   │   └── auth.js           # JWT 인증 미들웨어
│   ├── server.js             # Express 서버
│   └── package.json
│
├── frontend/
│   ├── src/
│   │   ├── pages/
│   │   │   ├── Home.jsx
│   │   │   ├── Login.jsx
│   │   │   ├── Register.jsx
│   │   │   ├── Dashboard.jsx
│   │   │   ├── ReportDetail.jsx
│   │   │   ├── Pricing.jsx
│   │   │   └── DealerDashboard.jsx
│   │   ├── components/
│   │   │   └── Header.jsx
│   │   ├── services/
│   │   │   └── api.js
│   │   └── styles/
│   │       └── global.css
│   ├── vite.config.js
│   └── package.json
│
├── start-dev.sh              # 개발 서버 시작 스크립트
└── README.md
```

---

## 🔌 API 엔드포인트

### 인증
| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/api/auth/register` | 회원가입 |
| POST | `/api/auth/login` | 로그인 |
| GET | `/api/auth/me` | 내 정보 조회 |
| POST | `/api/auth/apply-dealer` | 딜러 신청 |

### 검사/리포트
| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/api/reports/scan` | 웹사이트 검사 시작 |
| GET | `/api/reports` | 검사 목록 조회 |
| GET | `/api/reports/:id` | 검사 상세 조회 |
| GET | `/api/reports/:id/pdf` | PDF 다운로드 |
| POST | `/api/reports/:id/send-email` | 이메일 발송 |

### 결제
| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/api/payments/request` | 결제 요청 생성 |
| POST | `/api/payments/confirm` | 결제 승인 |
| POST | `/api/payments/test-payment` | 테스트 결제 |
| GET | `/api/payments/history` | 결제 내역 |

### 딜러
| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/dealers/dashboard` | 딜러 통계 |
| GET | `/api/dealers/customers` | 추천 고객 목록 |
| GET | `/api/dealers/commissions` | 수수료 내역 |

---

## ⚙️ 환경 변수

### backend/.env

```env
# 서버
PORT=5000
NODE_ENV=development

# 데이터베이스 (없으면 Mock 모드)
DATABASE_URL=postgresql://user:pass@localhost:5432/medical_comply

# JWT
JWT_SECRET=your-secret-key

# Anthropic Claude API (선택)
ANTHROPIC_API_KEY=sk-ant-xxx

# 토스페이먼츠 (선택, 없으면 테스트 모드)
TOSS_CLIENT_KEY=test_ck_xxx
TOSS_SECRET_KEY=test_sk_xxx

# Resend 이메일 (선택)
RESEND_API_KEY=re_xxx

# 프론트엔드 URL
FRONTEND_URL=http://localhost:5173
```

---

## 🚢 프로덕션 배포

### 1. 데이터베이스 설정

```bash
# PostgreSQL 데이터베이스 생성
createdb medical_comply

# 스키마 적용
psql medical_comply < backend/config/schema.sql
```

### 2. 환경 변수 설정

```bash
# .env 파일 생성
cp backend/.env.example backend/.env
# 실제 API 키로 수정
```

### 3. 프론트엔드 빌드

```bash
cd frontend
npm run build
```

### 4. 서버 실행

```bash
cd backend
npm start
```

---

## 📊 딜러 수수료 구조

| 항목 | 내용 |
|------|------|
| 수수료율 | **50%** |
| 월 구독료 | 99,000원 |
| 딜러 수익 | 49,500원/건 |
| 정산일 | 매월 15일 |
| 최소 정산 | 50,000원 |

---

## ⚠️ 면책 조항

본 서비스는 웹사이트의 의료법 준수 여부를 참고용으로 분석합니다.
정확한 법적 판단을 위해서는 **의료법 전문 변호사**의 자문을 받으시기 바랍니다.

---

## 📄 라이선스

MIT License

---

## 🤝 문의

- 이메일: support@medicalcomply.com
- 웹사이트: https://medicalcomply.com
